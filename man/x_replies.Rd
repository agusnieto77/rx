% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{x_replies}
\alias{x_replies}
\title{Fetch replies to a specific user.}
\usage{
x_replies(session, username, limit = NULL, mode = "latest", quiet = FALSE)
}
\arguments{
\item{session}{An \code{xtweetsR_session} object returned by \code{\link[=x_session]{x_session()}}.}

\item{username}{A single non-empty character string with an X
username (without the leading @).}

\item{limit}{Optional integer limiting the maximum number of reply posts
returned. When \code{NULL} (default), no limit is applied.}

\item{mode}{Optional character string: \code{"latest"} or \code{"top"}.
When provided, sets the X search mode. Defaults to \code{"latest"}
(equivalent to \code{f=live}).}

\item{quiet}{Logical, default \code{FALSE}. When \code{TRUE}, progress messages
are suppressed.}
}
\value{
A tibble with the canonical post schema (26 columns)
containing only reply posts where the specified user was mentioned.
Returns a zero-row tibble when no replies are found.
}
\description{
Searches X for posts mentioning a user (via \verb{@username}) and returns only
the posts that are actual replies.  Internally this reuses the same
network-capture → parse → normalize → deduplicate pipeline as
\code{\link[=x_search]{x_search()}} and \code{\link[=x_post]{x_post()}}.
}
\details{
Unlike \code{\link[=x_search]{x_search()}}, which returns all matching posts, this function
filters the result set to include only posts where \code{is_reply} is \code{TRUE}.
This makes it useful for tracking conversations where a specific user
is being replied to.
}
\examples{
\dontrun{
  sess <- x_session()
  replies <- x_replies(sess, "rstudio")
  replies_top <- x_replies(sess, "rstudio", mode = "top")
  print(replies)
  x_close(sess)
}

}
