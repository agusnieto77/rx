% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{x_save}
\alias{x_save}
\title{Save a post tibble to disk.}
\usage{
x_save(posts, path)
}
\arguments{
\item{posts}{A tibble with the canonical post schema (as returned
by \code{\link[=x_search]{x_search()}}).}

\item{path}{Character string with the output file path. The
extension determines the format (\code{.parquet}, \code{.duckdb}, or \code{.jsonl}).}
}
\value{
Invisible NULL when the target format is written successfully,
or the fallback \code{.jsonl} path (character) invisibly when an optional
dependency is missing and data was written as JSONL instead.
}
\description{
Writes a post tibble (as returned by \code{\link[=x_search]{x_search()}}) to a file.
The file format is inferred from the file extension:
}
\details{
\itemize{
\item \code{.parquet} — writes using the Arrow package (if available).
\item \code{.parquetds} — writes a partitioned Arrow dataset of Parquet
files (if Arrow is available).  Data is partitioned by
\code{collection_id} and the date extracted from \code{collected_at}.
\item \code{.duckdb} — writes using the DuckDB package (if available).
\item \code{.jsonl} — writes using jsonlite (always available, same as
\code{.rx_jsonl_write()}).
}

Unsupported or unrecognized extensions raise an error.
}
\section{Optional dependencies}{
Parquet support requires the \code{arrow} package. If Arrow is not
installed, calling \code{x_save()} on a \code{.parquet} file falls back
to JSONL with a warning and a \code{.jsonl} extension.

DuckDB support requires the \code{duckdb} package. If DuckDB is not
installed, calling \code{x_save()} on a \code{.duckdb} file falls back
to JSONL with a warning and a \code{.jsonl} extension.
}

\examples{
\dontrun{
  sess <- x_session()
  posts <- x_search(sess, "r programming", limit = 10)
  x_save(posts, "results.parquet")
  x_close(sess)
}

}
