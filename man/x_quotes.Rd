% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{x_quotes}
\alias{x_quotes}
\title{Fetch quote tweets of a specific post.}
\usage{
x_quotes(session, post_id, limit = NULL, mode = "latest", quiet = FALSE)
}
\arguments{
\item{session}{An \code{xtweetsR_session} object returned by \code{\link[=x_session]{x_session()}}.}

\item{post_id}{A character string with a bare post ID or a full
X/Twitter post URL.}

\item{limit}{Optional integer limiting the maximum number of quote posts
returned. When \code{NULL} (default), no limit is applied.}

\item{mode}{Optional character string: \code{"latest"} or \code{"top"}.
When provided, sets the X search mode. Defaults to \code{"latest"}
(equivalent to \code{f=live}).}

\item{quiet}{Logical, default \code{FALSE}. When \code{TRUE}, progress messages
are suppressed.}
}
\value{
A tibble with the canonical post schema (26 columns)
containing only quote tweets of the specified post.
Returns a zero-row tibble when no quotes are found.
}
\description{
Searches X for posts that quote a given post (by post ID or URL) and
returns only the posts that are actual quote tweets.  Internally this
reuses the same network-capture → parse → normalize → deduplicate
pipeline as \code{\link[=x_search]{x_search()}}, \code{\link[=x_post]{x_post()}}, \code{\link[=x_thread]{x_thread()}}, and \code{\link[=x_replies]{x_replies()}}.
}
\details{
Unlike \code{\link[=x_search]{x_search()}}, which returns all matching posts, this function
filters the result set to include only posts where \code{is_quote} is \code{TRUE}
and \code{quoted_post_id} matches the target post.  This makes it useful for
tracking how a specific post is being quoted across the platform.
}
\examples{
\dontrun{
  sess <- x_session()
  quotes <- x_quotes(sess, "1234567890123456789")
  quotes_top <- x_quotes(sess, "1234567890123456789", mode = "top")
  print(quotes)
  x_close(sess)
}

}
