{
  "name": "xtweetsr-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript sidecar for the xtweetsR R package — handles browser automation and structured data extraction.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
